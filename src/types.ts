export type student = {
    id?: number,
    name: string,
    email: string,
    birth_date: Date,
    class_id: number,
 }

 export type teacher = {
    id?: number,
    name: string,
    email: string,
    birth_date: Date,
    class_id: number,
 }

 