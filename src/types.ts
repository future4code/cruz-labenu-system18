export type student = {
    id: number,
    name: string,
    email: string,
    birth_id: Date,
    class_id: number,
 }

 export type teacher = {
    id: number,
    name: string,
    email: string,
    birth_id: Date,
    class_id: number,
 }

 